const { nanoid } = require("nanoid");
const products = require("./data/products.json");

const inform = console.log;

function run() {

    
    switch(userInput) {
        case "index":
        break;
        case "show":
        break;
        case "create":
        break;
        case "update":
        break;
        case "destroy":
        break;
    }
}